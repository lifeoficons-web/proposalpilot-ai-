# Database (required for production on Vercel - use Turso/libsql)
# Local dev: defaults to file:local.db
DATABASE_URL=
DATABASE_AUTH_TOKEN=

# OpenAI (required for proposal generation)
OPENAI_API_KEY=

# JWT Secret (optional - uses fallback in dev)
JWT_SECRET=

# PayPal (for payment processing)
NEXT_PUBLIC_PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
